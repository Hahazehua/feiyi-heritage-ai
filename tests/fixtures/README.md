# Test fixtures

Tests create malformed CSV copies under pytest-managed temporary directories so the repository never contains ambiguous invalid product data. `tests/evaluation_cases.json` contains the versioned MVP evaluation requests.
